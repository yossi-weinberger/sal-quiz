/**
 * Survey groups (1–100): one group per active product line after physical merges
 * (Nutrilion 22–24, diapers 35–40).
 */
export function productToGroupId(productId: number): number {
  return productId;
}
